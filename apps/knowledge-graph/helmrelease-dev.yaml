apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: knowledge-graph-dev
  namespace: flux-system
spec:
  targetNamespace: verus-dev
  interval: 5m
  chart:
    spec:
      chart: ./helm/knowledge-graph
      sourceRef:
        kind: GitRepository
        name: verus-elixir-dev
        namespace: flux-system
      interval: 1m
      valuesFiles:
        - helm/knowledge-graph/values-dev.yaml
  values:
    image:
      tag: 0.1.1-alpha.12-521b4b1 # {"$imagepolicy": "flux-system:knowledge-graph-dev:tag"}
